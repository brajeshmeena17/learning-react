# learning-react
- trying to learn git first
  ready reconers:
    - git status
    - git add .
    - git commit -m "msg"
    - git push 
    -     git push --set-upstream origin feat/news

    - switch b/w branches : git checkout branch_name
    - new branch : git checkout -b branch_name

    - git pull origin (from_branch)  -> run this in (to) branch
- day 1 : LEARNING JAVASCRIPT